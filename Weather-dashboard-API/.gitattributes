# Auto-detect text files and perform LF normalization
* text=auto

# Force shell scripts to always use LF line endings
*.sh text eol=lf

# Force configuration files to use LF line endings
*.example text eol=lf
.env* text eol=lf
*.conf text eol=lf
*.config text eol=lf

# Force Python files to use LF line endings
*.py text eol=lf

# Force YAML/JSON files to use LF line endings
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf

# Force Terraform files to use LF line endings
*.tf text eol=lf
*.tfvars text eol=lf
